package _21y._09m._09d_4;

public class OperatorPriority {

    public static void main(String[] args) {
        // [] (typ) `new` reference pretypovani, index, pretypovani
        // ! ++ -- ~
        // * - %
        // + -
        // << >> >>>\
        // < <= > >=
        // == !=
        // &
        // ^
        // |
        // &&
        // ||
        // ? : (ternární)
        // = += -= *= ....
    }
}
