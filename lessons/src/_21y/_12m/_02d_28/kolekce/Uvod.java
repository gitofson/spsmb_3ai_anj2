package _21y._12m._02d_28.kolekce;

/**Kolekce (kontejnery) - složí k uschování většího, předem
 * neznámého množství objektů. Njpoužívanější představitelé:
 * ArrayList - pole proměnné délky (někdy nazývané seznam).
 *      Připomíná klasické pole, pro přístup  k jednotlivým
 *      prvkům lze používat indexy. Zásadní rozdíl oproti
 *      klasickému poli je, že na počátku není nutné deklarovat
 *      počet prvků (pole je "nafukovací").
 * HashSet - množina. Obsahuje pouze unikátní prvky (nic se
 *      neopakuje). Pro přístup k prvkům nelze použít index.
 *      Pro přístup k prvkům je nutné použít některý z iterátorů.
 * HashMap - mapa. Též asociativní pole. Sada prvků typu klíč -
 *      hodnota. Pomocí klíče vyhledáváme daný prvek. Můžeme
 *      se na mapu též dívat jako na tabulku o dvou slopcích,
 *      kde jeden je primární klíč.
 *
 *
 */
public class Uvod {
}
